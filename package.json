{
  "name": "ice-cream-entrepreneur",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build --base=/ice-cream-entrepreneur/ && touch dist/.nojekyll"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
